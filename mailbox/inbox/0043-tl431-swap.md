---
task_id: "0043"
title: Swap D1 TLV431 to TL431 in solar-gate v1
expect-reply-within: 6h
---

# 0043 — Swap D1 TLV431 -> TL431 in solar-gate v1

## Context
0039 (solar-gate v1 schematic + autoroute) is verified and archived. During review you correctly flagged that the spec's "TLV431 2.5 V" was wrong -- TLV431's Vref is 1.24 V, so the REF2V5/VTH targets did not hold. Stephen's decision: swap D1 to TL431 (true 2.5 V reference), keeping the divider values that were computed for 2.5 V. The bias-current implications get worked out later -- this task is the swap only.

## Task
On a new branch `hermes/0043-tl431-swap`, branched from the 0039 tip (`ff28c10054fe4d100729a4a80588bee3e6e3c288`):

1. In `hardware/solar-gate-v1/`, replace D1 (TLV431) with TL431. Use SOT-23 if a TL431 SOT-23 symbol/footprint exists in the project library; if the footprint changes, update the board accordingly.
2. Re-run ERC -- must be 0 errors / 0 warnings. Quote the output.
3. If the board changed, re-run DRC and quote the full output.
4. Update the BOM (D1 line).
5. Report the quiescent/bias-current implication of TL431 vs TLV431 for the panel-powered reference: datasheet cathode-current min/typ for each, and what the reference branch draws. Numbers only -- do NOT redesign the bias network or re-scale the divider.
6. Push `hermes/0043-tl431-swap`.

## Success criteria
1. D1 is TL431 in the schematic, the board, and the BOM.
2. ERC 0 errors / 0 warnings after the swap (output quoted).
3. DRC output quoted after any board change.
4. Current-draw comparison reported with datasheet numbers.
5. Branch `hermes/0043-tl431-swap` exists on origin.

## Constraints
- Test board only. No Gerbers, no fab output, no ordering.
- Do not redesign the bias network or re-scale the divider -- report numbers only. Stephen said the deeper implications get worked out later.
- Stephen still reviews the schematic and layout before anything real is built.

## Proof
- Branch `hermes/0043-tl431-swap` + tip SHA on origin, ERC output, DRC output, BOM D1 line.

## Reply format
Per the canonical staged-reply format.
